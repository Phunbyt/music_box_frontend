import React from 'react'
import classes from './Header.module.css'

const Header = () => {
    return (
        <div className={classes.Header}>
            <h2 className={classes.Text}>Music Box</h2>
        </div>
    )
}

export default Header
